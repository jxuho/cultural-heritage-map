services:
  mongodb:
    image: mongo:latest
    container_name: mongodb_dev
    hostname: localhost
    ports:
      - "27017:27017"
    command: ["mongod", "--replSet", "rs0", "--bind_ip_all"]
    volumes:
      - mongo_data:/data/db
volumes:
  mongo_data: